export type Product = {
    id: string;
    name: string;
    tag: string;
    discount: string;
    price: number;
    oldPrice?: number;
    imageUrl?: string;
    sellerId: string;
    createdAt: string;
};
