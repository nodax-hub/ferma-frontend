export type ProductBatch = {
    id: string;
    productId: string;
    sellerId: string;
    manufacturedAt: string;
    quantity: number;
    initialQuantity: number;
    createdAt: string;
    updatedAt: string;
};
